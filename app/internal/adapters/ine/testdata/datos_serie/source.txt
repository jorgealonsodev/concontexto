Fixture: EPA453100.json
URL: https://servicios.ine.es/wstempus/js/ES/DATOS_SERIE/EPA453100?nult=3&tip=A
Fetched: 2026-07-28

Trimmed real response (nult=3 -- last three periods only). Verified live
during exploration (Engram #4690, "Cross-check: PRD §11.3's sample
response shows 2026-Q2=9.87 and 2026-Q1=10.83 for tasa-de-paro-epa. Live
EPA453100 returns exactly those, independently confirming the
identifier."). This is the "Tasa de paro (EPA)" series, quarterly
(expected frequency Q), table 65349.

The COD in this filename ("EPA453100") is the fixture's identity -- the
test suite in ../*_test.go derives it from this filename rather than
hard-coding it as a Go string literal, keeping the origin-identifier
static-scan guard (app/internal/guard) honest: real identifiers stay
confined to /config and testdata/, per the spec editorial-config
requirement, never as a Go source literal.

Amended 2026-07-29 (slice 2a, not a re-fetch): the original trim omitted
T3_TipoDato even though the request already carried tip=A (so the field
was present in the real response). Added "T3_TipoDato": "Definitivo" to
all three rows -- verified against the byte-identical values already
carried by app/internal/ingestion/testdata/datos_serie/tasa-de-paro-epa.json
(same COD, same three periods/values, already recorded Definitivo for
this series) -- and removed the phantom "Secreto" field, which does not
exist in the tip=A response either (spec source-ingestion-ine, "No
phantom field survives").
